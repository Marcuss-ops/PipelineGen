{
  "$schema": "archcheck-strict-baseline/v1",
  "$schema_changelog": "v2",
  "$v2_breaking_changes": [
    "transitional_baselines renamed to transitional_baselines_sorted_urgency (no compat alias; no consumers to break today per $v2_consumers)",
    "retired_entries renamed to audit_ledger (no compat alias; no consumers today)",
    "Each entry now carries urgency field (closed enum pending — ad-hoc labels today)"
  ],
  "$v2_consumers": "none_today (Phase 0 archcheck --strict is report-only); scripts/ci-architectural-checks.sh references this file's path only in comments, not as a load. Any future jq consumer must update path selectors.",
  "description": "Transitional baseline for cmd/archcheck --strict gate (PR-I, June 2026). Fail-closed per godlike/08 §zero-baseline rule: any past-deadline entry produces a hard CI exit-1 at --strict evaluation. Each entry MUST carry owner + deadline + removal_trigger (sorted_urgency: soonest first) or it is rejected. Refresh by Step 6 PR-G.3 CUTOVER: the internal/application/scripts entry from PR-G.2 BACKFILL is moved out of transitional_baselines into the audit_ledger (scripts split shipped, file_count dropped below cap). The internal/application/youtube entry was never a baseline member — explicit ledger row documents the prior baseline mention.",
  "transitional_baselines_sorted_urgency": [
    {
      "urgency": "soonest",
      "rule": "duplicate-types-allowlist",
      "failing_entries": [],
      "owner": "qdrant-hygiene Work Stream (Wave 20 AWK→Go rewrite)",
      "deadline": "2026-07-25",
      "removal_trigger": "AWK→Go helper deployed + zero violations reported by Check 5"
    },
    {
      "urgency": "primary",
      "rule": "max_files_per_package",
      "failing_packages": [
        {"package": "internal/app", "file_count": "exceeds 40", "max": 40},
        {"package": "internal/infrastructure/qdrant", "file_count": "exceeds 40", "max": 40}
      ],
      "owner": "PR-G.4+ refactoring stream (composition-root + qdrant adapter split)",
      "deadline": "2026-08-01",
      "removal_trigger": "Both packages ≤40 files each (cmd/archcheck --strict pkg_size rule reports zero)."
    },
    {
      "urgency": "primary",
      "rule": "max_lines_per_file",
      "failing_entries": "27 files exceeding 500 lines",
      "owner": "PR-G.4+ file-size split work stream",
      "deadline": "2026-08-01",
      "removal_trigger": "Each file ≤500 lines (cmd/archcheck --strict file_size rule reports zero)."
    },
    {
      "urgency": "primary",
      "rule": "max_constructor_deps",
      "failing_entries": "9 New<X>(...) signatures with >8 params (images service 17; assets/sourcing service 15; voiceover service 12; api/server 10; others 9-10 — exact list in cmd/archcheck --strict output)",
      "owner": "complexity-budget stream per godlike/08 §'Complexity budgets'",
      "deadline": "2026-08-01",
      "removal_trigger": "Each constructor ≤8 params (cmd/archcheck --strict constructor_deps rule reports zero)."
    },
    {
      "urgency": "primary",
      "rule": "cmd_main_max_lines",
      "failing_entries": "cmd/archcheck/main.go, cmd/worker/main.go (both >200 lines)",
      "owner": "cmd-binaries maintainers stream",
      "deadline": "2026-08-01",
      "removal_trigger": "Both cmd/<bin>/main.go ≤200 lines (cmd/archcheck --strict thin_command rule reports zero)."
    },
    {
      "urgency": "deferred",
      "rule": "stale_prose_paths_<stem>",
      "failing_entries": "Stem list defined in architecture/policy.yaml::stale_prose_paths (compose_voiceover, module_artlist, module_fullimages, compose_content, compose_images, module_ingest, module_jobs, module_stock, module_youtube) — counts vary by write-cycle; tracked at the rule-family level not per-stem. Step 6 CUTOVER retired 4 module_jobs prose references in composition_test.go; stems list unchanged because module_jobs still names live code (e.g. internal/app/module_jobs_test.go).",
      "owner": "doc-debt cleanup stream",
      "deadline": "2026-08-15",
      "removal_trigger": "Stems list shrinks as files genuinely rename (e.g. compose_voiceover→internal/app/build_bundles_voiceover.go, module_jobs→internal/app/build_bundles_jobs.go). Trigger is rename-only — current prose rewrites cannot run ahead of the underlying rename; rewriting to a name that doesn't yet exist is doc drift per godlike/07."
    }
  ],
  "audit_ledger": [
    {
      "ledger_kind": "transitional_retired",
      "rule": "max_files_per_package",
      "package": "internal/application/scripts",
      "was_count": 55,
      "was_deadline": "2026-07-10",
      "was_owner": "PR-G mega-package split work stream",
      "retired_by": "Step 6 PR-G.3 CUTOVER (commit pending — drives this baseline refresh)",
      "retired_in_commit_chain": [
        "Step 5 PR-G.2 BACKFILL: a4394411 → push e74735d4 (June 2026)"
      ],
      "removal_trigger_met": "Split into 8 subpackages (adapters/ contracts/ dto/ events/ jobs/ jsonextract/ ports/ usecase/); root contains zero .go files; total files per subpackage under cap-40.",
      "schema_note": "This ledger entry carries 7 of 11 godlike/07 deprecation-record fields: id (synthesized: PR-G.2-BACKFILL-RETIRE), owner_capability (PR-G mega-package split work stream), replacement (adapters/+contracts/+dto/+events/+jobs/+jsonextract/+ports/+usecase/), introduction_date (2026-06-22 — first transitional baseline registration), removal_date (2026-06-28 — five days early vs. 2026-07-10 deadline), tracking_issue (PR-G.2 BACKFILL ticket, see architecture/current.yaml), compatibility_test (go build ./... + go vet ./... + go test ./internal/application/scripts/... all GREEN; archival — no live readers post-Step-5)). Three fields elided: exact_symbol (single mega-package, no symbol-level deprecation), file:line (whole-package move, not file-by-file), usage_metric (zero consumer reads post-Step-5 — verify via rg 'Marcuss-ops/PipelineGen/internal/application/scripts\"$' --glob '!*test*' . returning 0). Ledger format is NON-canonical — migrated to godlike/07 deprecation-record shape on next wave."
    },
    {
      "ledger_kind": "non_member_retired",
      "rule": "max_files_per_package",
      "package": "internal/application/youtube",
      "was_count": "43 per prior narrative (was NOT a transitional_baselines member; prior baseline JSON referenced it descriptively in description field only)",
      "retired_from_reference": "Step 6 PR-G.3 CUTOVER (commit pending); description string dropped the youtube reference",
      "schema_note": "Was never a rules-bearing member; the prior baseline JSON description text mentioned it but it was never in `transitional_baselines.failing_packages`. This ledger row exists to record the audit decision (per godlike/07 §zero-legacy deprecation-record discipline) that the prior mention was non-binding and not a transitional baseline entry."
    }
  ]
}
