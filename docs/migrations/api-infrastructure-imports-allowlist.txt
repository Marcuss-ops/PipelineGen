# docs/migrations/api-infrastructure-imports-allowlist.txt
# ─────────────────────────────────────────────────────────────────────────────
# Per-file allowlist for Check 19 of `scripts/ci-architectural-checks.sh`,
# promoted from soft-log to hard-fail evidence-based by commit
# `ci/archcheck-hard-fail` (June 2026).
#
# HOW THIS FILE WORKS
# ────────────────────
# Check 19 scans `internal/api/` (excluding `_test.go`) for any file whose
# source contains a real Go import of a package under
# `github.com/Marcuss-ops/PipelineGen/internal/infrastructure/`. The list of
# files that match is subtracted (via `comm -13`) against the paths listed
# below. Any leftover match fails the gate with `exit 1`.
#
# FORMAT
# ──────
# One repo-root-relative path per line. Comments start with `#` and are
# ignored. Groups of entries MAY be preceded by a section header comment
# (the header itself starts with `#` so the gate skips it).
#
# POLICY
# ──────
# * Each grand-parented import must ship with a migration plan tracked
#   under the corresponding `architecture/migration.yaml` wave (or PR
#   track for in-flight work).
# * Adding a NEW entry REQUIRES team review (no solo adds).
# * Removing an entry is always safe; the gate will fail if the underlying
#   import is still present, surfacing the regression for action.
#
# CURRENT STATE — Wave 14 grandfathered imports
# ──────────────────────────────────────────────
# The entries below are real Go imports of internal/infrastructure/*
# (verified via `rg -l 'github\.com/Marcuss-ops/PipelineGen/internal/infrastructure/'
# internal/api --glob '*.go' --glob '!*_test.go'`). The archcheck gate compares
# this file symmetrically with that scan, so stale entries fail too. They are grandfathered
# because Wave 14 (API compaction, June 2026) is in flight: the
# `internal/api/` subtree already dropped Drive, Realtime, SearchQueries,
# FullImages, Workers, and Sources — but the in-flight asset handlers
# (clips, artlist, youtube), the script orchestrator, and the channel/image
# middleware still legitimately need access to internal/infrastructure
# adapters for upload, Drive binding, and DB-backed policy checks.
#
# Migration plan (per architecture/current.yaml Wave 14, June 2026):
#   * Wave 14-PR2: extract bulk_upload + handler.go + workers out of
#     api/assets/clips/* → internal/application/assets/clips/{service,worker}.
#     This removes 8 entries.
#   * Wave 14-PR3: extract api/script/{flow,handler_flow,handler_jobs,helpers}
#     → internal/application/scripts/{flow,service,worker} per docs/CHANGELOG_2026-06-03.md
#     §"ScriptFlow helper extraction".
#   * Wave 14-PR4: api/system/{handler,handler_drive,module} →
#     internal/application/system/* handlers (no infra reach-through).
#     Removes 3 of the 27.
#   * Wave 14-PR5: api/{image,middleware,server,routes,module_core_modules,soundeffect}
#     thin-transport rewriting per docs/api-package-boundaries.md.
#     Removes the remaining 12.
#
# Once the corresponding PR lands, the file path here MUST be REMOVED in
# the same PR (or in a followup) so the gate starts enforcing zero leaks
# for that file.
#
# CURRENT STATE (P0.6, June 2026): ZERO active entries.
# The internal/api/ layer is now clean of infrastructure imports — every
# file that previously had one was migrated to typed ports during Wave 14
# (PR2-PR5, June 2026) and the P0.4+P0.5 SearchAggregator/ReuploadUseCase
# extraction. Check 19 now enforces zero-baseline: any new infrastructure
# import in internal/api/ fails the gate. Additions to this file require
# explicit owner + deadline per AGENTS.md §8.
# ─────────────────────────────────────────────────────────────────────────────

# ── Wave 14 grandfathered: api/assets/artlist handlers (PR2 — CLEANED, June 2026) ──
# Internal cleanup wave, June 24 2026: api/assets/artlist/artlist_handlers.go
# no longer imports internal/infrastructure. The handler now depends on
# `artlist.ArtlistConfigPort` (a 1-method typed port defined in
# internal/application/assets/providers/artlist/ports.go); the concrete
# adapter wrapping `*config.Config` lives in
# internal/app/artlist_config_adapter.go (the only composition-root site
# allowed to wire infrastructure types into api ports). Removed from
# allowlist at PG-004-cleanup.
# internal/api/assets/artlist/artlist_handlers.go     ← removed from allowlist

# ── Wave 14 grandfathered: api/assets/clips handlers (PR2) ─────────────────
# helpers.go removed at W14-clips-helpers-extract (June 2026): the
# conversion helpers ClipToAssetNode + TreeNodeToAssetNode (previously
# internal/local `treeNodeToAssetNode`) were lifted to
# internal/application/clips/conversions.go (canonical seam above
# infra). Four api callers now route through appclips.<Func>:
#   clip_update.go    :: Handler.UpdateClip
#   clip_create.go    :: Handler.CreateClip
#   clip_upload.go    :: Handler.UploadVideoClip
#   folder_tree.go    :: Handler.GetFolderChildren + Handler.GetTree
# (Line-number citations deliberately omitted: the gate tokenizes on
# file paths only, while comment-side line numbers drift on edit.)
# Remaining PR2 grandfathered entries: 1.
# internal/api/assets/clips/handler.go     ← removed from allowlist (P0.6, June 2026)
#                                            handler.go was cleaned of all
#                                            infrastructure imports during the
#                                            P0.4+P0.5 ReuploadUseCase extraction +
#                                            SearchAggregator mandatory migration.

# ── Wave 14 grandfathered: api/assets/soundeffect + youtube handlers (PR2 — CLEANED, June 2026) ─
# Internal cleanup wave, June 24 2026: api/assets/soundeffect/handler.go +
# api/assets/youtube/youtube_handlers.go no longer import internal/infrastructure.
# Sfx depends on 4 sfxports.* interfaces (ClipRepositoryPort, DriveUploaderPort,
# SemanticMetadataWriterPort, DestinationResolverPort); youtube depends on
# ytports.ClipStorePort. Concrete adapters live in internal/app/adapters_soundeffect.go
# + internal/app/adapters_youtube.go respectively. Removed from allowlist at
# PG-003-cleanup.
# internal/api/assets/soundeffect/handler.go     ← removed from allowlist
# internal/api/assets/youtube/youtube_handlers.go ← removed from allowlist

# ── Wave 14 grandfathered: api/channels + api/images impl (PR5) ────────────

# ── Wave 14 grandfathered: api/middleware (PR5 — CLEANED, June 2026) ────────
# Internal cleanup wave, PG-006 (June 2026): every middleware file under
# internal/api/middleware/ now consumes typed ports (AuthSecurityPort,
# RateLimitPort, FeatureFlagsPort) declared in
# internal/application/middleware/ports.go. Concrete adapters wrapping
# `*config.Config` live in internal/app/middleware_security_adapter.go.
# The Logger middleware now holds the typed RequestLogSink port (defined
# in internal/application/middleware/requestlog) instead of *sql.DB; the
# SQLite implementation lives in internal/infrastructure/database/sqlite/logsink
# and never crosses the api/middleware import boundary.
# Bootstrap entry-point: internal/app/bootstrap.go no longer imports
# internal/infrastructure/* (concrete wiring moved to databases_helpers.go,
# drive_folders_resolve.go and wire_services.go in the same package).
# Removed from allowlist at PG-006-cleanup.
# internal/api/middleware/admin_token.go            ← removed from allowlist
# internal/api/middleware/middleware_feature_flags.go ← removed from allowlist
# internal/api/middleware/middleware_logger.go       ← removed from allowlist
# internal/api/middleware/middleware_middleware.go   ← removed from allowlist
# internal/api/middleware/middleware_ratelimit.go    ← removed from allowlist

# ── Wave 14 grandfathered: api root files (PR5 — CLEANED, June 2026) ────
# Internal cleanup wave, June 26 2026: module_core_modules.go, routes.go,
# server.go no longer import internal/infrastructure — removed from allowlist.

# ── Wave 14 grandfathered: api/script orchestrator (PR3 — CLEANED, June 2026) ─
# Internal cleanup wave, June 24 2026: api/script/handler_flow.go,
# handler_jobs.go} no longer import internal/infrastructure — the handlers
# were already wired via canonical application ports (DocumentCreator,
# DocumentCreator) defined in handler_flow.go itself, mirroring the
# Wave 14 PR3 migration plan. Removed from allowlist at PR3-cleanup.
# (helpers.go was already removed earlier — see the inline note below.)
# Agente 4 (June 2026): helpers.go no longer imports internal/infrastructure
# (config dep was moved to the composition root). The Check 19 hard-fail
# gate now enforces a clean transport for this file.
# internal/api/script/helpers.go  ← removed from allowlist

# ── Wave 14 grandfathered: api/system handlers (PR4 — CLEANED, June 2026) ──
# Internal cleanup wave, June 24 2026: api/system/{handler.go, handler_drive.go,
# module.go} no longer import internal/infrastructure. The handler takes
# (1) a DoctorConfig struct (replacing *config.Config), (2) a Reconciler
# port (replacing *drivecleanup.Service), and (3) a DriveAdminOps port
# (replacing *drive.Uploader). Concrete adapters live in
# internal/app/system_adapters.go (the only place allowed to wire
# infrastructure types into api ports). Removed from allowlist at PR4-cleanup.

# Grandfathered clips handlers (PR2) — pending complete extraction
internal/api/assets/clips/folder_query_handler.go
internal/api/assets/clips/handler.go
internal/api/assets/clips/ingest.go
internal/api/assets/clips/nonops/handler.go
internal/api/assets/clips/ops.go
internal/api/assets/clips/search.go
