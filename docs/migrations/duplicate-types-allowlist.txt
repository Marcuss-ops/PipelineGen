# docs/architecture/godlike/duplicate-types-allowlist.txt
#
# Transitional allowlist for scripts/ci-architectural-checks.sh Check 5
# (same-package duplicate-type-declarations lint).
#
# Format: `<dir>/<package>:<TypeName>   # <rationale> — owner: <handle>, deadline: <YYYY-MM-DD>`
# The lint scanner reads the first whitespace-separated token only:
#   grep -vE '^\s*(#|$)' duplicates-types-allowlist.txt | awk '{print $1}' | sort -u
# Adding/removing an entry MUST ship in the same PR as the code change that
# requires it. Per AGENTS.md §8 ARCHITECTURE-CI-GATES zero-baseline rule,
# the final acceptable count is zero; transitional baselines require an
# explicit owner + deadline. New entries WITHOUT those columns fail the
# governance gate.
#
# ┌──────────────────────────────────────────────────────────────────────────┐
# │ POST-LENS-MIGRATION (PRE-EXISTING-15-LENS-MIGRATION, July 2026)             │
# │                                                                          │
# │ The Check 5 scanner keys each declaration as `<dir>/<pkg>:<type>` where    │
# │ `dir` is the directory_path (NOT just package_name — matches Go's         │
# │ package-identity contract per architecture/policy.yaml). Each allowlist  │
# │ entry MUST use the full `dir/pkg:type` form to match scanner keys; bare  │
# │ `pkg:type` form is functionally inactive (cannot exempt the scanner's   │
# │ count>=2 verdict). Multi-dir declarations (different dirs sharing the    │
# │ same package name) REQUIRE one line per directory, since each             │
# │ (dir,pkg,type) tuple is a distinct scanner key post PRE-EXISTING-15-     │
# │ LENS-MIGRATION. This migration closes the project-wide allowlist-key-    │
# │ format mismatch flagged in the prior code-review (commit 55a0f56eb).     │
# └──────────────────────────────────────────────────────────────────────────┘
#
# Current baseline (origin/main, July 2026): see `wc -l` for the live
#   line count. Post-P1-7 the PHASE-A.2-PR-KERNEL-JOB-POPULATE section is
#   ENTIRELY retired (atomic cutover of internal/domain/job/ →
#   internal/kernel/job/, 2026-07-30). Before P1-7 the section hosted 30
#   entry lines (15 paired `internal/domain/job:*` + `internal/kernel/job:*`
#   cells) plus its 8-line section header block; both are gone now.
#   The matching canonical-side kernel/job cells were REMOVED FROM THIS
#   ALLOWLIST TOGETHER with the domain/job cells rather than left as
#   zero-source exemptions — a Check 5 ground-truth sweep reconfirmed
#   they are zero-sources in current tree, so they need no exemption.
#   Operators auditing churn: the prior 71-line / 38-entry baseline is in
#   git history at HEAD~1.
#   - 13 CHECK 5 LOG entries (21 lines: 4 single-dir (scripts:*×3 +
#     outbox:IndexClipper) + 9 multi-dir split × 2 cells = 22 cells,
#     minus 1 cell api-side images:GenerateImageRequest drop = 21 actual)
#   - (legacy 7+9 taxonomy from prior baseline merged into the 13
#     entries above; the 10 PR-check-5 baseline-closure multi-dir axioms
#     split are now visible per-line in the section body)
#   - 10 PR-CHECK-5-FOLLOWUP entries (each split into 2 lines = 20 lines)
#   - 15 PHASE-A.2-PR-KERNEL-JOB-POPULATE entries (each split into
#     2 lines = 30 lines: domain/job/ + kernel/job/)
# Deadline (whole block): 2026-09-01.
# After 2026-09-01 these MUST migrate per godlike/07 §"Migration sequence"
# (EXPAND → BACKFILL → CUTOVER → CONTRACT); the allowlist may NOT shrink
# via silent expiry — each entry is ratcheted individually.
#
# DROPPED in this commit (post-CHECK-5-LENS ground-truth sweep):
#   - engine:Generate — rg confirmed 0 `^type Generate` hits in current
#     codebase; the canonical engine surface now lives at
#     internal/application/scripts/usecase/engine.go (per PR-CHECK-24-FIXUP,
#     2026-07-08). Check 24's tolerance for missing engine.go makes the
#     engine-side type extinct; the cross-package-NAME rationale is no
#     longer applicable. Removal: owner = @jobs-core, deadline = 2026-09-01.
#
# Owner groups used below:
#   @qdrant-hygiene           — legacy QDRANT-projection mirrors
#   @jobs-core                — application/jobs + scripts/jobs + voiceover/jobs job-system handlers
#   @system-health-team       — application/system/health (port) + infrastructure/health (impl)
#   @voiceover-team           — capabilities/voiceover canonical sync/types surface (migrated from application/voiceover Aug 2026)
#   @voiceover-reconciliation — reconstruction deprecation shims (PR 6 + reconciliation/voiceover)
#   @scripts-adapters         — application/scripts/adapters module-local service structs
#   @images-team              — application/images (canonical DTO) + api/images (HTTP transport DTO)
#   @youtube-team             — application/youtube (canonical) + assets/providers/youtube (provider-adapter mirror)
#   @completion-team          — assets/completion (projector) + jobs/completion (broker spine)
#   @qdrant-team              — qdrant-side canonical (reconciler/maintenance) vs assets-side cross-package-NAME mirrors
#   @translation-team         — application/translation (ports, canonical; domain/translation retired Aug 2026)
#
# ┌──────────────────────────────────────────────────────────────────────────┐
# │ PURGE (2026-09-11, ARCHCHECK-W1-TRUTHFUL-GOVERNANCE)                       │
# │                                                                          │
# │ ALL 36 active entries were REMOVED. Every one had deadline 2026-09-01    │
# │ (expired) and the Check 5 scanner reports ZERO duplicate-type            │
# │ violations without them, so each was a zero-source exemption — the       │
# │ file was telling a false story.                                          │
# │                                                                          │
# │ Why the entries were inert:                                              │
# │   - 30 pointed at `internal/application/**` and `internal/api/**`.       │
# │     Those trees no longer exist (application/api → capabilities/kernel   │
# │     migration), so the scanner keys could never match a real             │
# │     declaration.                                                         │
# │   -  6 pointed at live directories (platform/health, sqlite/jobs,        │
# │     capabilities/voiceover, capabilities/scripts/jobs) but exempted      │
# │     declarations that no longer exist.                                   │
# │                                                                          │
# │ Per AGENTS.md §8 zero-baseline rule the final acceptable count is zero.  │
# │ This file now carries NO active entries. A NEW entry requires an         │
# │ explicit owner + a FUTURE deadline shipped in the same PR as the code    │
# │ change it exempts.                                                       │
# │                                                                          │
# │ Audit trail: the pre-purge 36-entry body is in git history at HEAD~1.    │
# └──────────────────────────────────────────────────────────────────────────┘
#
# ◷ CHECK 5 LOG SECTION (PR-check-5 baseline closure, June 2026, post-LENS migration) ◷
# Entries here were originally documented as cross-package-NAME false
# positives (the legacy pkg:Type lens; superseded by
# PRE-EXISTING-15-LENS-MIGRATION). Each line was one (dir,pkg,type) scanner
# key; multi-dir declarations occupied multiple consecutive lines.
#
#   [ALL ENTRIES PURGED 2026-09-11 — see the PURGE block above.]
#
# ◷ PR-CHECK-5-FOLLOWUP (July 2026, post-LENS migration) ◷
# 10 cross-package-NAME false positives (each split into 2 lines = 20 lines).
# Each entry documented a distinct (dir,pkg,type) scanner key; multi-dir
# declarations occupied consecutive lines. The pkg names accidentally
# collided across sibling directories (e.g. `reconciler` appeared in both
# `application/assets/deletion/reconciler` and `application/qdrant/reconciler`);
# godlike/06 SSOT resolved them as distinct concerns.
#
#   [ALL ENTRIES PURGED 2026-09-11 — see the PURGE block above.]
#
#
# ── OUT-OF-SCOPE DEFERRED PAIRS (PR-check-5 follow-up, June 2026) ──
# The Check 5 lint also flagged 3 additional pairs that are NOT in the
# user's PR-check-5 scope. Each requires its own owner + deadline + a
# future-blocco disposition; tracking as separate follow-up tickets:
#
#   [none remaining — three OOS pairs above have been migrated into the
#    active allowlist proper as of June 2026 PR-check-5 follow-up closure.
#    The OOS section is retained as a marker for churn history.]
#
# These three OOS pairs were intentionally NOT added in the original
# baseline — they required per-pair code-or-allowlist resolution in a
# future PR and adding them under the same owner@handle as the 7 in-scope
# entries would muddy the audit trail. Migration to active allowlist
# (with owner + deadline per godlike-08 zero-baseline rule) completes the
# PR-check-5 deferred-work disposition.
#
# ── POST-MIGRATION CHURN HISTORY (commit this-PENDING migration) ──
# - engine:Generate: dropped. rg confirmed zero `^type Generate` hits in
#   current codebase after PR-CHECK-24-FIXUP (2026-07-08) removed the legacy
#   internal/application/scripts/engine.go. The canonical engine surface
#   now lives at internal/application/scripts/usecase/engine.go.
#   Owner migration: @jobs-core (deadline 2026-09-01 — satisfied by removal).
# - voiceoversync:{Service,Summary}: dropped both cells of each. The
#   voiceover-shim refactor (PR-VOICEOVER-SYNC-CONTRACT-CUTOVER, 2026-07-10)
#   funneled all alias-shims through `internal/application/voiceover/`
#   directly (no `voiceover/sync/` subdir anymore); the reconciliation
#   re-export path became redundant. Owner: @voiceover-reconciliation.
# - search:Service: dropped both cells (assets/search/ + youtube/search/).
#   The cross-module mirror split into `search.Logger` (still alive on both
#   sides) and a dedicated `search.Service` named differently per side;
#   the allowlisted name is no longer accurate. Owner: @qdrant-hygiene.
# - images:GenerateImageRequest api-side cell: dropped. The api-side
#   binding was migrated out of `api/images/impl.go` and into a router-level
#   schema during the images-team restructure (PR-IMAGES-API-ROUTER,
#   2026-07-12); the canonical DTO at application/images/ports.go persists.
#   Owner: @images-team.
# - scripts:SQLite mirrors: rationale STALE per FASE-X scripts refactor;
#   entries reduced to single-dir form (application/scripts/ only).
#   rg-confirmed: 3 hits in `internal/application/scripts/{usecase,adapters,ports}`
#   subdirs, all under `application/scripts/` package.
# - All other entries: SPLIT into per-dir (dir,pkg,type) lines so the
#   (dir,pkg)-keyed Check 5 scanner can find them.
