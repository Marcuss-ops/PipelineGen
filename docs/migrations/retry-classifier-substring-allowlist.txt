
# docs/migrations/retry-classifier-substring-allowlist.txt
# Format: <relative-path-without-leading-dot>
# Each line is one file path exempted from Check 50 (retry-classifier substring-matcher).
# The package-level allowlist (pkg/retry, pkg/textutil, pkg/similarity) is hardcoded in the
# script; this file is the per-file transitional-baseline allowlist that absorbs known
# offenders so the gate can land atomically.
#
# Per AGENTS.md §8 ARCHITECTURE-CI-GATES zero-baseline rule: every entry requires explicit
# owner + deadline + rationale. Transitional baselines default-block the lint rather than
# silently pass. Migration of any allowlist entry to the canonical typed-path surface
# (retry.IsTransient + retry.WrapTransient) MUST happen in a separate atomic commit and
# remove the line from this file.
#
# Forward-pointer PR-MIGRATE-ARTLIST-ISRETRYABLE-TO-TYPEDPATH (owner: artlist-infrastructure,
# deadline 2026-07-25): wrap the underlying YTDLPDownloader / HTTPDownloader errors with
# retry.WrapTransient at the call site; replace downloader.isRetryable with retry.IsTransient
# + a typed deny-list (errors.Is for context.Canceled / DeadlineExceeded).

internal/infrastructure/artlist/downloader/downloader.go	# artlist-infrastructure	2026-07-25	isRetryable deny-list substring matcher for 4xx codes (400/401/403/404/410); typed-path migration gated on upstream error wrapping.
# Additional offenders caught at Check 50 introduction time (June 2026).
# Migration plan below per AGENTS.md godlike/08 zero-baseline rule.
internal/capabilities/youtube/jobs/classify.go	# youtube-classify	2026-07-25	YouTube classify.go has typed result classifier that uses substring matching for retryable 4xx/5xx detection; typed-path migration gated on iterative classifier closure.
internal/platform/drive/uploader.go	# drive	2026-07-25	IsRetryable uses substring matching despite 6f327b10 typed-wrap at SDK exit (typed path now augments but substring classifier is the existing fallback per PR-VO-A3 / pre-Step-7 contract); typed-path migration requires design decision on whether substring predicate is canonical for HitRateLimit vs RateLimit semantic.
internal/infrastructure/youtube/ollama_clip_metadata_builder.go	# youtube-ollama	2026-07-25	Ollama metadata builder has retry-classifier substring basenameMatch pattern; typed-path migration gated on upstream LLM-error wrapping.
