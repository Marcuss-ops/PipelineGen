# API architectural violations

Snapshot of production-code references that cross the API boundary through
`*sql.DB`, `net/http`, `os`, or `RootFolderOverride`.

| Package | Weighted severity |
|---|---:|
| `internal/api` | 4 |
| `internal/api/admin` | 1 |
| `internal/api/assets/clips` | 7 |
| `internal/api/middleware` | 19 |
| `internal/api/transport` | 7 |
| **Total** | **38** |

## Priority

`internal/api/middleware` is the dominant API-layer hotspot. New work should
introduce typed transport ports rather than adding concrete HTTP or persistence
dependencies to handlers.

Exact file and line evidence is intentionally generated by the architecture
scanner. Static line references are not duplicated here because refactors make
them stale immediately.
