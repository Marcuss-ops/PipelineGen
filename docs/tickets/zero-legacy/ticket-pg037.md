# PG-037

Stato: parziale. Solo `main`; nessuna branch o PR.

Migrare i consumer ai contratti canonici, rimuovere API deprecate, alias e forwarding wrapper, aggiornare test, eseguire i controlli, rebase su `origin/main`, `git push origin main`.
