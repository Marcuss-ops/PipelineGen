# PG-040

Stato: aperto. Solo `main`; nessuna branch o PR.

Dividere il sourcing service, mantenere un solo registry e fallback, aggiornare test, eseguire i controlli, rebase su `origin/main`, `git push origin main`.
