# PG-024

Stato: completato. Solo `main`; nessuna branch o PR.

Verificare che le route script duplicate e il wrapper legacy non ricompaiano. Correggere solo regressioni, rebase su `origin/main`, `git push origin main`.
