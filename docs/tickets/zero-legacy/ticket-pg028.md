# PG-028

Stato: aperto. Solo `main`; nessuna branch o PR.

Dividere composition.go per capability, mantenere un solo composition root e registry condivisi, aggiornare test, eseguire i controlli, rebase su `origin/main`, `git push origin main`.
