# PG-015 — Capability immagini non implementate

Stato: completato.

Regola operativa: usare esclusivamente `main`; nessuna branch e nessuna PR. Verificare soltanto che le capability eliminate e la configurazione collegata non ricompaiano. Una regressione reale va ribasata su `origin/main` e pubblicata con `git push origin main`.
