# PG-032

Stato: aperto. Solo `main`; nessuna branch o PR.

Compattare Artlist mantenendo un solo orchestratore e registry, separare responsabilità, aggiornare wiring e test, eseguire i controlli, rebase su `origin/main`, `git push origin main`.
