# PG-022

Stato: parziale.

Lavorare solo su `main`, senza branch o PR.

Completare il provisioning folder Drive fuori dal bootstrap: definire DTO e porte tipizzate, implementare use case idempotente e adapter, centralizzare i nomi, rimuovere query e mutazioni di configurazione dal bootstrap, aggiornare test, eseguire i gate, fare rebase su `origin/main` e usare `git push origin main`.
