# PipelineGen — Piano Zero Legacy

Tutti i ticket si eseguono esclusivamente su `main`. Nessuna branch e nessuna PR. Seguire `00_GLOBAL_RULES.md`.

Stati e ordine operativo sono verificati sul codice corrente prima di ogni ticket. Un solo writer alla volta. Il ticket successivo parte dopo la verifica del commit su `origin/main`.
