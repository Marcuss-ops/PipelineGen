# PG-044

Stato: parziale. Solo `main`; nessuna branch o PR.

Ridurre l’orchestratore YouTube, eliminare stato duplicato e callback residue, mantenere un solo writer, aggiornare test, eseguire i controlli, rebase su `origin/main`, `git push origin main`.
