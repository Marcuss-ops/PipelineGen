# PG-039

Stato: aperto. Solo `main`; nessuna branch o PR.

Separare startup, worker registration, shutdown e health da lifecycle.go, mantenere un solo owner, aggiornare test, eseguire i controlli, rebase su `origin/main`, `git push origin main`.
