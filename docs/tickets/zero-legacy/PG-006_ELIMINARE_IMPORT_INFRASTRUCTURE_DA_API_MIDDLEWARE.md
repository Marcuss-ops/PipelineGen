# PG-006

Stato: completato. Solo `main`; nessuna branch o PR.

Verificare che il middleware API resti basato su porte tipizzate. Correggere solo regressioni, rebase su `origin/main`, `git push origin main`.
