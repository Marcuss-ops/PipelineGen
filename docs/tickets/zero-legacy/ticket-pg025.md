# PG-025

Stato: completato. Solo `main`; nessuna branch o PR.

Verificare che tracker risolti, riferimenti obsoleti e conteggi vecchi non ricompaiano. Correggere solo regressioni, rebase su `origin/main`, `git push origin main`.
