# PG-047

Stato: aperto. Solo `main`; nessuna branch o PR.

Creare un registry statico unico dei comandi admin, derivare help e dispatch dal registry, eliminare switch duplicati, aggiornare test, eseguire i controlli, rebase su `origin/main`, `git push origin main`.
