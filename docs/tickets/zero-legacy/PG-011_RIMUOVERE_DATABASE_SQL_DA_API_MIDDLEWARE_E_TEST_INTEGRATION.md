# PG-011

Stato: completato.

Regola operativa: usare esclusivamente `main`. Non creare branch e non aprire PR. Prima del push fare rebase su `origin/main`; pubblicare con `git push origin main`.

Questo ticket richiede solo una verifica anti-regressione.
