# PG-036

Stato: aperto. Solo `main`; nessuna branch o PR.

Mantenere un solo dispatcher outbox, eliminare wrapper e duplicazioni, aggiornare wiring e test, eseguire i controlli, rebase su `origin/main`, `git push origin main`.
