# PG-043

Stato: aperto. Solo `main`; nessuna branch o PR.

Separare query, mapping, serializzazione e delivery da metadata_export, mantenere un solo dispatcher, aggiornare test, eseguire i controlli, rebase su `origin/main`, `git push origin main`.
