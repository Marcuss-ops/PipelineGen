# PG-029

Stato: parziale. Solo `main`; nessuna branch o PR.

Separare DTO, porte, use case e helper da scripts/types.go, eliminare residui e alias, aggiornare wiring e test, eseguire i controlli, rebase su `origin/main`, `git push origin main`.
