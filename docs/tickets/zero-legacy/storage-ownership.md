# PG-019

Stato: aperto.

Lavorare solo su `main`, senza branch e senza PR.

Obiettivo: mantenere un solo proprietario degli handle storage. Eliminare campi duplicati, migrare composition e lifecycle, centralizzare la chiusura, aggiornare i test, fare rebase su `origin/main` e pubblicare con `git push origin main`.
