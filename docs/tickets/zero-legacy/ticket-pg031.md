# PG-031

Stato: aperto. Solo `main`; nessuna branch o PR.

Ridurre internal/app, eliminare file orfani e duplicazioni, mantenere un solo composition root e una sola costruzione per adapter, eseguire i controlli, rebase su `origin/main`, `git push origin main`.
