# PG-033

Stato: parziale. Solo `main`; nessuna branch o PR.

Completare la tipizzazione dei wiring, eliminare slot generici e cast, riutilizzare porte canoniche, aggiornare test, eseguire i controlli, rebase su `origin/main`, `git push origin main`.
