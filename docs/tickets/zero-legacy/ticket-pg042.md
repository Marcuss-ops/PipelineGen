# PG-042

Stato: parziale. Solo `main`; nessuna branch o PR.

Tipizzare la pipeline script senza cambiare i nove step, aggiornare test, eseguire i controlli, rebase su `origin/main`, `git push origin main`.
