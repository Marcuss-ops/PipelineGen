[Service]
# Semantic embedding is outside the Chronon frame lane. Keep the render GPU
# free for Vulkan/CUDA/NVENC unless an embedding-specific workload opts in.
Environment=PIPELINEGEN_EMBEDDING_DEVICE=cpu
