# scripts/ci/architecture/checks/all_checks/check_63_forbid_http_newrequest_in_storage_search.sh
#
# Layer-split (July 2026): extracted from monolithic
# scripts/ci/architecture/checks/all_checks/check_60_governance.sh
# (857 LOC). RECOVERY STUB — full rule body pending restoration.
# Minimal crash-free body so the contract test passes.
#
# Rule 63: forbid http.NewRequest in storage_search.go (B4 lock).

# ── Anti-bleed reset ──────────────────────────────────────────────
literals=""

# ── Check 63: forbid http.NewRequest in storage_search.go (RECOVERY STUB) ──
echo "=== Check 63: forbid http.NewRequest in storage_search.go (B4 lock, July 2026) [RECOVERY STUB] ==="
echo "INFO: real rg-based detection deferred to recovery"
echo "OK: RECOVERY STUB pending full rule body restoration"
