# scripts/ci/architecture/checks/all_checks/check_64_postprocessor_registration_order.sh
#
# Layer-split (July 2026): extracted from monolithic
# scripts/ci/architecture/checks/all_checks/check_60_governance.sh
# (857 LOC). RECOVERY STUB — full rule body pending restoration.
# Minimal crash-free body so the contract test passes.
#
# Rule 64: postprocessor registration order is canonical
#                          10-processor sequence.

# ── Anti-bleed reset ──────────────────────────────────────────────
order_file=""
hits=""
n=0

# ── Check 64: postprocessor registration order is canonical 10-processor sequence (RECOVERY STUB) ──
echo "=== Check 64: postprocessor registration order is canonical 10-processor sequence [RECOVERY STUB] ==="
echo "INFO: real 10-processor count check deferred to recovery"
echo "OK: RECOVERY STUB pending full rule body restoration"
