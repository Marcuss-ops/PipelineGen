refactor(archcheck): split main.go into report.go + command.go (PR1 of 4)

FASE 1.B of the 5-step P0 split: scripts/archcheck/main.go (1.349 righe)
→ modular scripts/archcheck/ (foundation first, then check logic in PR2-4).
PR1 stabilisce la foundation (Report struct + CLI flag parsing) PRIMA di
estrarre il runner e le 5 check_* in baseline/ e checks/. Coesistenza con
main.go esistente: tutti i ~1.200 LOC di check logic restano in place
fino a PR3/PR4 (rimozione finale quando il trim main.go ≤100 viene
raggiunto).

Foundation (3 nuovi file + 1 golden + 1 test, 0 logica spostata):

- report.go (~75 LOC): Report struct (verbatim) + EncodeReport helper
  che centralizza json.Encoder.SetIndent. Snapshot test in snapshot_test.go
  esercita la forma del wire via os.Pipe capture, indipendente da main()
  (che chiama os.Exit e non è importabile da un test nello stesso package).

- command.go (~90 LOC): Config struct + Mode enum (ModeFocused /
  ModeRatchet / ModeSeedBaseline) + ParseCommandLine(args, errSink) helper.
  Mode.String() preserva i valori originali ("focused", "ratchet",
  "seed-baseline" - quest'ultimo nuovo ma usato solo come constant label,
  mai fluente in report.Mode perché seed-baseline short-circuit-a prima
  della costruzione del Report).

- testdata/report_schema.json: golden file mappante field Report -> tipo
  JSON atteso. Documenta l'header "_optional" per i field `json:",omitempty"`
  (focused_gate_passed, legacy_budget) - la loro assenza dall'output encoded
  è legittima quando zero. Aggiornare in concomitanza con ogni modifica a
  Report struct o EncodeReport.

- snapshot_test.go (~190 LOC): 3 test - TestReportSchema_Focused /
  TestReportSchema_Ratchet / TestEncodeReport_WireFormat. captureEncodeReport
  usa il pattern canonico "wait-then-close + concurrent drain" via
  os.Pipe + doppio goroutine per evitare la race writer-closed-too-early
  (chiusura di pw prima che json.Encoder.Encode finisca di scrivere).

- main.go (1.349 → 1.337 LOC): main() slimmed a ParseCommandLine +
  EncodeReport. runSeedBaseline estratto dal branch inline di
  *seedBaseline (più leggibile + testabile). Report struct rimosso (ora
  in report.go). Foundation section (package doc + imports + main() +
  runSeedBaseline) finisce a riga 107 - sopra il cap 100 di ~7 righe
  perché il package doc originale è wordy e merita di restare intatto
  finché non viene trim-mato in PR4 insieme al resto del check logic.

Verifica (5/5 spec):

(a) go build ./scripts/archcheck: ✓ (binary produces).
(b) go run ./scripts/archcheck --ratchet: diff vs pre-refactor = vuoto
    (stessa Report shape). Stesso per default = focused mode.
(c) snapshot_test.go: 3 nuovi test, tutti green (più i 7 esistenti in
    gate/gate_test.go che restano intatti).
(d) go vet ./scripts/archcheck/...: ✓ clean.
(e) wc -l cap: tutti i nuovi file sotto 200 LOC; main.go resta > 100
    perché il check logic non è ancora stato estratto (per quello serve
    PR3/PR4).

Workflow AGENTS.md Git-Lesson-2/3: commit direct-to-main, Co-authored-by
trailer, push via git fetch && git rebase origin/main && git push origin
main (no --force). Stash+pop del go.mod unrelated per sbloccare il rebase.

Nota per il prossimo agent: PR2 (runner.go + baseline/{load,compare,seed}.go)
e PR3 (checks/{api_imports,database_sql}.go) sono i prossimi step logici
della FASE 1.B. PR4 chiude con checks/{deprecations,dependency_edges}.go
+ trim di main.go a ≤100 LOC totali.

Co-authored-by: PipelineGen Agent <agent@pipelinegen.local>
