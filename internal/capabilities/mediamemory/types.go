// Package mediamemory is the canonical capability owner for
// the MediaMemory subsystem: visual resolution, concept binding,
// candidate discovery, batch materialization, linking, and
// feedback.
//
// Package doc + SlotKind alias — canonical home in capabilities.
package mediamemory

import (
	"github.com/Marcuss-ops/PipelineGen/internal/kernel/media"
)

// SlotKind is an alias for the canonical media.SlotKind.
type SlotKind = media.SlotKind