package catalog

// Canonical catalog job type constants.
// Per godlike/02 capability-specific constants live in their owning domain package.
const (
	// TypeSync is the canonical job type for catalog synchronization.
	TypeSync = "catalog.sync"
)
