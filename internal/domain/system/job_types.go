package system

// Canonical system job type constants.
// Per godlike/02 capability-specific constants live in their owning domain package.
const (
	// TypeCleanup is the canonical job type for system cleanup.
	TypeCleanup = "system.cleanup"
)
