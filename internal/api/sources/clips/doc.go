// Package clips owns the unified clip HTTP surface (CRUD, upload,
// folders, search, delete, reconcile, verify, cleanup). Consolidation
// finished in PR-A phase 4-min + sub2 + BULK (commits 14a35665…5d338c17).
package clips
