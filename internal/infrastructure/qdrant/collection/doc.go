// Package collection is the qdrant capability slice for collection management
// (schema definitions, retention, blue-green switch, collection_info).
//
// EXPAND Tier-1 scaffolding (June 2026): empty sub-package established as the
// canonical home for collection-side files migrated in the subsequent BACKFILL
// phase. Per godlike/07 §migration sequence, content migration is gated and
// shipped as a follow-up commit.
package collection
