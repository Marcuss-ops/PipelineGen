// Package connection is the qdrant capability slice for connection lifecycle
// (client transport, config, and connection-pool plumbing).
//
// EXPAND Tier-1 scaffolding (June 2026): empty sub-package established as the
// canonical home for connection-side files migrated in the subsequent BACKFILL
// phase. Per godlike/07 §migration sequence, content migration is gated and
// shipped as a follow-up commit with the typed-port contract audited.
package connection
