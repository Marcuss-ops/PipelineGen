// Package reaper is the qdrant capability slice for the point reaper
// (locator cleaner, payload merge / redact path, lifecycle-driven point culling,
// dead-letter adapter).
//
// EXPAND Tier-1 scaffolding (June 2026): empty sub-package established as the
// canonical home for reaper-side files migrated in the subsequent BACKFILL
// phase. Per godlike/07 §migration sequence, content migration is gated and
// shipped as a follow-up commit.
package reaper
