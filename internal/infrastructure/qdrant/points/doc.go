// Package points is the qdrant capability slice for point-level operations
// (upsert, search, query/searcher, payload indexes, index writer/document).
//
// EXPAND Tier-1 scaffolding (June 2026): empty sub-package established as the
// canonical home for points-side files migrated in the subsequent BACKFILL
// phase. Per godlike/07 §migration sequence, content migration is gated and
// shipped as a follow-up commit.
package points
