server:
  port: 8080
  gin_mode: "release"

storage:
  data_dir: "./data"

external:
  ollama_url: "http://localhost:11434"

paths:
  node_scraper_dir: "../node-scraper"
  python_scripts_dir: "../../python"

drive:
  stock_root_folder: ""
  clips_root_folder: ""

harvester:
  drive_folder_id: ""

logging:
  level: "info"
  format: "json"
