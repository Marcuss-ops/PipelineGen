# Deprecation registry index — informational aggregate manifest of non-empty
# records/ shards. The validator reads records/*.yaml + audit.yaml directly;
# empty bucket files are intentionally not tracked.
#
# Capability migration note (Aug 2026): owner_capability paths moved from
# internal/application/* to internal/capabilities/*. The categorizer still
# maps legacy prefixes for not-yet-migrated records, while buckets with zero
# records simply have no shard.
schema_version: 1
source_directory: architecture/deprecations
total_records: 7
records:
    - bucket: assets
      file: records/assets.yaml
      count: 2
    - bucket: misc
      file: records/misc.yaml
      count: 4
    - bucket: qdrant
      file: records/qdrant.yaml
      count: 1
audit_file: audit.yaml
