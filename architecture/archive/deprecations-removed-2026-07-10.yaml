# Archive of deprecation records that reached status: removed.
# Migrated from architecture/deprecations.yaml per the legacy-cleanup action plan
# (architecture/action-plans/2026-07-10-legacy-cleanup-5-item-orchestration.md, Item 4).
#
# Format follows the existing archive pattern at
# architecture/archive/deprecations-removed-2026.yaml.
#
# Each record preserves its canonical surface unchanged (godlike/06 SSOT:
# one canonical owner per fact). The deprecations.yaml file is the active
# SSOT for status: active and status: deprecated records; this archive
# file is the SOLE SSOT for status: removed records.
#
# When a record reaches status: removed in deprecations.yaml, it must be
# migrated to this file (or a future per-quarter archive file) and removed
# from deprecations.yaml in the same atomic commit. The action plan
# §3.4 documents the 4-house rule: stable structure + parse-cleanly + one
# canonical owner + cross-references in CHANGELOG.md + AGENTS.md.

deprecations:
  - id: DRIVE-STORE-UPLOAD-TO-DRIVE
    owner_capability: internal/infrastructure/drive
    exact_symbol: "(*Store).UploadToDrive — calls s.driveUploader.UploadFile() directly"
    file: internal/infrastructure/drive/store.go
    file_line: 180
    replacement: "delivery.Publisher.Publish(ctx, PublishRequest{Destination: DestinationImage, LocalPath: filePath, Filename: filename, ...})"
    introduction_date: 2026-07-01
    removal_date: 2026-07-06
    tracking_issue: "DRIVE-UPLOAD-ANALYSIS-2026-07-06 P0-2 (shipped 2026-07-06)"
    compatibility_test: "rg 'Store\\.UploadToDrive' internal/application/images/ returns 0 production hits. All 3 bridge methods (ImageStorageService.publishToDrive, fullimages.Service.publishToDrive, MetadataService.publishMetadata) now route exclusively through delivery.Publisher.Publish. Fail-closed on nil publisher."
    usage_metric: "Pre-migration: 3 call sites in images package (storage_service.go x1, fullimages/service.go x1, metadata_service.go x1) — all in bridge-method fallback paths. Post-migration: 0 production callers; bridge methods fail-closed on nil publisher."
    migration_phase: CONTRACT
    status: removed
    notes: "P0-2 (July 2026): all 3 bridge methods migrated to delivery.Publisher.Publish exclusively. mediaStore.UploadToDrive fallback removed from all 3. MetadataService.mediaStore field removed; fullimages.Service.mediaStore field removed. ImageStorageService.mediaStore kept (used elsewhere: storage_drive.go, storage_ingest_direct.go). Store.UploadToDrive method preserved (non-images callers may exist)."
    archived_at: 2026-07-10
    archived_by: "PR-DEPRECATIONS-ARCHIVE-2026-07-10 (legacy cleanup Item 4)"
