server:
  port: 8080
  public_base_url: "https://veloxmanager.duckdns.org"

storage:
  data_dir: "./data"

paths:
  credentials_file: "credentials.json"
  token_file: "token.json"
  clip_text_dir: ""

drive:
  stock_root_folder: ""
  clips_root_folder: ""

textgen:
  default_model: "gemma3:12b"
  timeout: 60
